/**
 * Composition root for application dependencies.
 * This will eventually assemble services, repositories, and infrastructure providers.
 */
export class AppContainer {
	constructor() {
		// Intentionally minimal for now.
	}
}
